import { createTheme } from "@mantine/core";

export const theme = createTheme({
    primaryShade: 7,
    primaryColor: "violet",
    fontFamily: "Inter, sans-serif",
    headings: {
        fontFamily: "Inter, sans-serif",
    },
});
